---
doc_type: goal-iteration
goal: fix-skills-update-and-standalone-version
iteration: 1
status_after: active
next_action: "启动独立 code review，处理全部 blocking/important 后执行终端功能验收。"
blocker_signature: null
updated_at: "2026-07-14"
---

# Iteration 001

## Current Understanding

#45 来自外部 `skills@1.5.17` 安装与 update 删除检测的 discovery 不同源；#47 来自 standalone 单元缺少版本载体且 runtime 将缺失降级为 `unknown` 成功路径。

## Implementation Approach

采用仓库可控的完整 package 重装入口解决 #45，并用离线 oracle 与真实 CLI E2E 锁定；为 #47 增加自包含 `VERSION`、发布/checker 一致性和 runtime fail-closed。

## Changes This Iteration

- 完成两项 issue 的 report、analysis、实现、回归与 fix-note。
- 更新双语安装/升级文档、release bump、plugin checker 与 runtime sync。
- 新增 standalone 与多-skill distribution 测试和 legacy inventory fixture。

## Verification Evidence

- focused RED 后转为 GREEN；相关 `45 passed, 1 skipped`。
- 真实固定 CLI E2E `1 passed`。
- 全量 `443 passed, 1 skipped`。
- plugin checker、runtime sync、ruff、diff check 和 Markdown ≤300 行均通过。

## Problems Encountered

初版 fail-closed 把 `version-unavailable` 放在 dirty managed paths 之前，相关回归捕获后已恢复原有用户改动保护优先级。

## Next Attempt

启动项目指定的 Claude Fable 高推理独立审查，按 findings 修复并复审，再做 Task agent 功能验收。

## State Update

Goal 保持 `active`，已完成 iteration 1，无 blocker。
